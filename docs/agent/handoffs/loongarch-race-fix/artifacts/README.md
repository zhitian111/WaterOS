# Artifact policy

No kernel binary or disk image is copied into this handoff. Those inputs are
large, ignored, locally reproducible, or contain competition fixtures. Their
paths, sizes, provenance, and available SHA-256 values are recorded in
`../HANDOFF.md` and `../snapshots/`.
