% 【本文件写什么】独立子 crate，无 api/impl 目录树：klog-impl
% - 职责：klog 存储后端，ringbuf。
% - 源码：os/components/wateros-klog/klog-impl/src/
% - 对外暴露的函数/类型、在聚合 lib.rs 中的重导出方式
% - 实现要点、feature，若有、与其它子 crate 的边界
% 【事实来源】
% - os/components/wateros-klog/klog-impl/、父组件 src/lib.rs

\subsection{klog-impl}

子crate\code{klog-impl/klog-ringbuf}，即包名 \code{wateros-klog-impl-ringbuf}，实现desc槽+每槽变长正文的环形存储。聚合层再导出\code{KlogRingbuf}；\code{init}/\code{record}/\code{iter\_from}等高层API在聚合\code{src/lib.rs}，经\code{KlogRingbuf::with}持锁访问内环。

\input{chapters/chap03/components/wateros-klog/klog-impl/impl/ringbuf}
