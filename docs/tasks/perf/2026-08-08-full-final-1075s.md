# 2026-08-08：完整 RISC-V Final BuildStorm 验收

## 结果

```text
BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=1075.25 cores=8 bytes=1681000 arch=riscv64
```

此前记录的最优完整 Final 为 `elapsed_s=1281.26`。本次 `1075.25`，下降约
`16.1%`。CAgent 10 项全部通过，BuildStorm toolchain/minibuild/compile 均成功。

## 本次包含的优化

- 页缓存连续池
- ext4 write-back cache
- ext4 批量释放
- block cache 连续块池
- mmap 空闲区间搜索跳转
- 绝对路径规范化单遍写入
- user_copy 读路径单次页表 walk

## 测试环境

- QEMU RISC-V64 virt
- 8 vCPU / 8 GiB
- `sdcard-rv-pub.img` + `-snapshot`
- P-core 绑定：`taskset -c 0,2,4,6,8,10,12,14`

## 材料

```text
raw_log: /tmp/final-after-perf-20260808.log
  sha256: 784b24e50e97e13c9a90c9822b272c27bd3f8be7fcd6f5baf5b2409fe3633902
```

## 结论

本轮性能优化组合已通过完整 Final 验收。后续：

- 继续跑 LoongArch Final 完整轮。
- 对剩余热点 `memcpy/memcmp`、`copy_to_user_progress`、VirtIO 做下一轮 A/B。
- 最终门禁仍需补齐 iozone/LTP/FS、`e2fsck -fn` 和掉电一致性。
