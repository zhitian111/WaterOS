[package]
    name = "wateros-task-scheduler-impl-rt-fifo"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    task-api = { path = "../../../task-api/api-v0/", package = "wateros-task-api-v0", default-features = true }

[profile.dev]
    panic = "abort"

[profile.release]
    panic = "abort"
