//! 用户态/内核共享的系统调用 ABI 数值常量（与陷阱帧参数槽布局一致）。
//! 参数编号从零开始；调用方不得传入超过该上限的槽位，否则入口会读取未定义的
//! 陷阱帧内容或错误地覆盖返回值。

/// 单次系统调用在寄存器/参数包中可见的最大参数个数。
///
/// 须与陷阱帧及用户态/内核 ABI 中参数数组长度一致。
pub const MAX_SYSCALL_ARGS: usize = 6;
