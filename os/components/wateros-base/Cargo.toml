[workspace]
    members = [ "base-config" ]
[package]
    name = "wateros-base"
    version = "0.1.0"
    edition = "2024"

[dependencies]
config = { path = "base-config", package = "wateros-base-config" }
spin = { version = "0.10.0", default-features = false, features = ["mutex", "spin_mutex"] }
