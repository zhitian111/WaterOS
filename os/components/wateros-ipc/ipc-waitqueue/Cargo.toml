[package]
    name = "ipc-waitqueue"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    wateros-task = { path = "../../wateros-task/", default-features = false }

[profile.dev]
    panic = "abort"

[profile.release]
    panic = "abort"
