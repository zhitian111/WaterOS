[package]
    name = "ipc-shm"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    api-v0 = { path = "../../wateros-mm/mm-api/api-v0/", package = "wateros-mm-api-v0", default-features = true }
    frame_alloctor = { path = "../../wateros-mm/mm-frame-alloctor", package = "wateros-mm-frame-alloctor", default-features = true }
    spin = "0.10.0"

[profile.dev]
    panic = "abort"

[profile.release]
    panic = "abort"
