[package]
    name = "wateros-runtime-logging"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    log = { version = "0.4.29" }
    console = { path = "../runtime-console/", package = "wateros-runtime-console", default-features = false }
[features]
    default = [  ]
    impl-debug = [  ]
    impl-dummy = [ "console/impl-dummy" ]
    impl-error = [  ]
    impl-info = [  ]
    impl-platform-console = [ "console/impl-platform-console" ]
    impl-trace = [  ]
    impl-warn = [  ]
