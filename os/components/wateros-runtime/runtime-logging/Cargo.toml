[package]
    name = "wateros-runtime-logging"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    log = { version = "0.4.29" }
    console = { path = "../runtime-console/", package = "wateros-runtime-console"}
[features]
    default = [ "impl-trace" ]
    impl-trace = []
    impl-debug = []
    impl-info = []
    impl-warn = []
    impl-error = []
