[package]
    name = "wateros-mm-impl-common"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    api-v0 = { path = "../../mm-api/api-v0/", package = "wateros-mm-api-v0", default-features = true }
    runtime = { package = "wateros-runtime", path = "../../../wateros-runtime", default-features = true }

[features]
    default = []

[profile.dev]
    panic = "abort"

[profile.release]
    panic = "abort"
